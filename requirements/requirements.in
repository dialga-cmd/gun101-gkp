# Direct dependencies for the CI lockfile (requirements/ci-lock.txt).
# Generated with: uv pip compile --universal --generate-hashes --python-version 3.9
# Supports the full CI matrix (Python 3.9 - 3.12).
#
# Build tools
pip
build
setuptools==70.3.0
wheel==0.43.0
-c constraints-build.txt
# Runtime
cryptography
# Test / lint / type / security
pytest
pytest-cov
hypothesis
ruff
mypy
bandit
pip-audit
