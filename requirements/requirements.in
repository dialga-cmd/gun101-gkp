# Direct dependencies for the CI lockfile (requirements/ci-lock.txt).
# Generated with: uv pip compile --universal --generate-hashes --python-version 3.10
# Supports the full CI matrix (Python 3.10 - 3.12).
#
# Build tools (setuptools/wheel versions pinned via ../constraints-build.txt)
pip
build
setuptools
wheel
-c ../constraints-build.txt
# Runtime
cryptography
# Test / lint / type / security
pytest
pytest-cov
hypothesis
ruff
mypy
bandit
pip-audit
